# 학습 에이전트 설계문서 (v0)

> 상태: **v0 (우리 가설)** — 2026-06-16 작성
> 다음 단계: 1시 세미나(청취·녹음) 후 "전문가 견해 vs 우리 가설" 오버레이 → v1 수렴

## 작업 워크플로우

```
[지금~] 우리 가설 v0 작성  →  [1시] 세미나(청취·녹음)  →  [이후] 녹음 오버레이(diff)  →  v1 수렴
```

- **원칙**: ① 문서 우선(이 파일에 누적) ② 갈림길마다 추천안+근거 제시 ③ 현재 LLM 구현 기반
- **세미나 처리**: 비동기. v0를 먼저 끝까지 밀어붙이고, 녹음이 오면 "수렴/반영/의도적 기각(이유 포함)" 표로 대조.

### 목차(예정)

1. 에이전트란 / 학습 에이전트란 — **(작성됨)**
1.5 우리 제품 정의 — 문제정의, Why Agent, 북극성 — **(작성됨)**
2. 학습자 모델 (학습 모델 + 삶 모델)
3. 에이전트 루프 / 워크플로우 (진단→계획→실행→성찰)
4. 행동·도구 정의 (Action space)
5. 아키텍처 (L1 LLMOps → L2 Agent 전환)
6. 평가·관측 (학습효과 측정 + 습관/지속)

---

# 1장. 정의

## 1.1 에이전트란 무엇인가

### 고전적 정의 (AI 일반)
> **에이전트(Agent)** = 환경을 **인지(perceive)** 하고, 목표를 달성하기 위해 그 환경에 **자율적으로 행동(act)** 하는 시스템.
> — 인지→판단→행동의 루프를 *스스로* 돈다는 점이 핵심 (Russell & Norvig).

### LLM 시대의 재정의
LLM 에이전트는 **LLM을 추론 엔진(reasoning engine)으로 삼아, 자신의 제어 흐름(control flow)을 스스로 결정**하는 시스템이다. 즉 "다음에 무엇을 할지"를 코드의 `if/else`가 아니라 모델의 판단이 정한다.

### 에이전트 판별 기준 (4가지)
무언가가 "에이전트인가?"를 가르는 질문들:

| 기준 | 질문 | 에이전트라면 |
|---|---|---|
| **① 자율성** | 다음 행동을 누가 정하나? | **모델이 동적으로** 결정 (코드가 아님) |
| **② 목표지향** | 무엇을 향해 움직이나? | 명시된 목표 상태로 수렴 |
| **③ 상태/기억** | 과거를 기억하나? | 세션·장기 메모리 보유 |
| **④ 도구·환경** | 외부에 영향을 주나? | 도구 호출로 환경과 상호작용 |

### 에이전트가 *아닌* 것 (대조)

| 형태 | 왜 에이전트가 아닌가 |
|---|---|
| 단일 LLM 호출 | 무상태, 자율성·목표 없음 (입력→출력 1회) |
| 자동화/RPA·스크립트 | 흐름이 완전 고정, 판단 없음 |
| **워크플로우(LLM 파이프라인)** | LLM·도구를 쓰지만 **경로가 코드로 미리 정해짐** |
| 일반 챗봇 | 반응형 대화, 목표·계획·자기수정 없음 |

> **워크플로우 vs 에이전트** — 같은 부품(LLM+도구)을 써도, *경로를 코드가 정하면 워크플로우, 모델이 정하면 에이전트*. 이것이 우리가 가려는 **L1(LLMOps) → L2(Agent)** 의 본질.

### 성숙도 사다리 (현재 위치)

| 단계 | 특징 | 다음 행동 결정자 |
|---|---|---|
| L0 단일 호출 | 무상태 | 사람(프롬프트) |
| **L1 LLMOps 파이프라인** | 체이닝·관측·평가, **경로 고정** | 개발자(코드) ← *현재 우리 위치* |
| **L2 에이전트** | 상태+도구+**동적 행동 결정** | 에이전트 자신 ← *목표* |
| L3 다중 에이전트 | 역할 분담·위임 | 오케스트레이터 |

---

## 1.2 학습 에이전트란 무엇인가

위 일반 에이전트 정의를 **학습 도메인**에 투영한다.

| 일반 에이전트 | → 학습 에이전트 |
|---|---|
| 환경(environment) | **학습자**(그의 지식 상태·반응) |
| 목표(goal) | 학습자를 현재 수준 → 목표 역량으로 이동 |
| 인지(perceive) | 답변·발화·오답·반응시간을 **진단**으로 해석 |
| 행동(act) | 교수 활동(입력 제시·연습·교정·복습) 선택·실행 |

### 핵심: "학습"의 이중성
이 도메인에서 'learning'은 두 방향을 동시에 의미하며, **둘 다 있어야** 학습 에이전트다.

| | A. 가르치는 학습 (Teaching) | B. 배우는 학습 (Adaptation) |
|---|---|---|
| 주체 | 에이전트가 **사용자를** 학습시킴 | 에이전트가 **사용자에 대해** 학습함 |
| 산출 | 다음 학습 활동 | 갱신된 학습자 모델·전략 |
| 빠지면 | 개인화 없는 추천기 | 매번 리셋되는 정적 튜터봇 |

> **A↔B 두 루프가 맞물려 도는 것** = LLMOps를 넘어서는 결정적 지점. (대부분의 "AI 튜터"는 A만 있고 B가 없다.)

### 한 문장 정의
> **학습 에이전트** = 영속적 **학습자 모델**을 들고, 매 턴 학습자 신호를 **진단**해, 학습과학 원리를 행동 선택지로 삼아 **다음 학습 활동을 스스로 계획·실행**하고, 그 결과로 **학습자 모델과 전략을 갱신**하는 폐루프 시스템.

### 학습 에이전트 판별 기준 (4기준의 학습 도메인 투영)

| 기준 | 학습 에이전트에서 |
|---|---|
| ① 자율성 | "지금은 새 입력? 연습? 복습? 교정?"을 **스스로** 선택 |
| ② 목표지향 | ZPD/*i+1* 기준 목표 역량으로 수렴 |
| ③ 상태/기억 | **학습자 모델**(약점·이력·망각곡선) |
| ④ 도구·환경 | 콘텐츠 생성·채점·SRS 스케줄러·자료검색(RAG) |

### 학습 에이전트가 *아닌* 것
- **정적 튜터 챗봇**: 매 세션 리셋, 학습자 모델 없음 (B 결여)
- **문제 추천 시스템**: 개인화는 하나 교수 루프·자기수정 없음 (A 결여)
- **LLMOps 콘텐츠 생성 파이프라인**: 경로 고정, 동적 행동 결정 없음 (현재 우리 위치)

### 우리가 만들 것 (범위)
**언어학습 메커니즘을 코어로 하는 범용 학습 에이전트.** 언어학습의 검증된 원리(이해가능한 입력·출력강제·인출연습·간격반복·비계설정)를 **행동 선택지(action space)** 로 삼되, 구조 자체는 타 과목으로 일반화 가능하게 설계.

---

---

# 1.5장. 우리 제품 정의

## 1.5.1 문제 정의
> 언어학습 = **내 생각을 외국어로 표현하는 것**. 따라서 **"나의 이야기"로** 공부해야 한다.

- "나의 이야기"를 받는 1차 수단 = **일기(텍스트+사진)** *(다른 방법도 가능)*
- **하루 5분, 꾸준히** — 저마찰 습관형 마이크로러닝

## 1.5.2 현재 플로우 (Stage 1 = 워크플로우)
1. 유저가 일기(텍스트·사진)를 쓴다
2. LLMOps가 내용 분석 → 한국어 문장 1~5개 생성
3. 한국어 문장 제시 → 유저가 **영어로 표현(production)**
4. **즉각 피드백**
5. (일상·학습 데이터 누적 → 점점 맞춤형) ← *여기가 에이전트의 자리*

## 1.5.3 학습과학 근거 (왜 이 설계가 유효한가)
| 우리 설계 | 검증된 원리 |
|---|---|
| 내 생각을 외국어로 "표현" | **출력 가설(Swain Output Hypothesis)** |
| "내 이야기"로 공부 | **자기참조 효과(self-reference effect)** |
| 한국어→영어 산출 | **강제 출력(pushed output)** / 인출 연습 |
| 즉각 피드백 | 형성 평가 + 교정 피드백 |
| 데이터 누적 → 맞춤형 | 적응형 학습 = **B루프(배우는 학습)** |

## 1.5.4 북극성 컨셉 — "AI가 인간을 강화학습한다"
주체를 뒤집은 RL: **인간이 학습되는 정책, AI가 환경+보상**.

| RL 요소 | 우리 앱 |
|---|---|
| Agent(학습 주체) | **인간(유저)** |
| Policy | 유저의 "외국어로 사고하는 습관" |
| Environment + Reward | **우리 AI** (문장 제시·피드백) |
| Goal | **체화·자동화** (통제적 처리 → 자동적 처리) |
| Curriculum | target selection (다음에 뭘 연습?) |

> **학습 에이전트의 정체 = 인간을 훈련시키는 "트레이너 겸 환경".** 일 = *체화*를 향해 커리큘럼·보상을 설계 (= 최적 교수 정책).

## 1.5.5 Why Agent — 에이전트의 존재 이유
**핵심 결정 = target selection**: *"이 학습자의 누적 상태를 보고, 이번에 무엇을 연습시킬 것인가."*
나머지(문장 생성·피드백)는 이 결정을 실행하는 **도구**. 두뇌만 에이전트, 손발은 워크플로우.

- ①~④단계 = 워크플로우 (고정 경로, 그대로 둠)
- "점점 맞춤형" = 에이전트 (코드로 못 적는 개인화 의사결정)
- 판별 테스트: *"다음 행동을 모든 학습자 공통 순서도로 그릴 수 있나? → 가능하면 워크플로우, 불가능하면 에이전트."*

## 1.5.6 제품 성숙도 3단계 (에이전트성 ↑)
```
Stage 1 [지금]   반응형 워크플로우       일기→문장→표현→피드백
Stage 2 [다음]   반응형 + 개인화 두뇌    에이전트가 target 결정 → 맞춤 → 모델 갱신
Stage 3 [북극성] 능동형(proactive)      유저 입력 없이 AI가 먼저 말 검 (when까지 자율 결정)
```

## 1.5.7 동기 설계 — 외적보상 배제 + 퍼즐=몰입 가설
**핵심 철학: 외적보상(점수·뱃지) 전면 배제.** 순수 내적동기 — *문장을 만들고 맞추는 쾌감*으로 스스로 유입.

### 근거 1) 자기결정이론(SDT)
외적보상은 내적동기를 밀어냄(과잉정당화). 배제가 정답. 내적동기 3요소를 이미 충족:
| SDT 욕구 | 우리 앱 |
|---|---|
| 유능감(competence) | 문장 맞춘 **쾌감** ← 핵심 동력 |
| 자율성(autonomy) | 내 이야기로 내가 씀 |
| 관계성(relatedness) | 능동형 말걸기(Stage 3) |

### 근거 2) 퍼즐 가설 = 몰입(Flow) 이론
> 창업 가설: **언어학습 = 퍼즐게임.** 규칙(문법) + 조각(단어) → 조합해 뜻 만들고 맞추기.

이는 **몰입(Csikszentmihalyi)** 과 동일: *난이도 ≈ 실력*일 때 몰입. 그리고 **몰입 = i+1**.
→ 직관(퍼즐의 재미)과 학습과학(i+1 난이도)이 같은 것.

### 효과↔지속 충돌의 해소 (이전 가설 정정)
이전 v0: "효과 vs 지속 충돌". → **퍼즐=몰입 철학이 이를 해소.**
```
        효과 ─┐
              ├─ 몰입(적정 난이도) ── 같은 지점에서 만남
        지속 ─┘   풀리는 쾌감 = 학습 = 다시 오게 만듦
```
**몰입 지점(난이도=실력)에서 효과와 지속이 수렴.** 외적보상 없이 "스스로 유입"되는 메커니즘.

### RL 프레이밍 정밀화
외적보상 없음 → RL의 보상 신호 = **내적보상(맞춘 쾌감)**. 따라서:
> AI = 보상 *분배기*가 아니라 **몰입 설계자(flow architect)**. 핵심 결정 = **"몰입을 유지하는 난이도의 퍼즐(target) 고르기."**
→ 학습자 모델은 *"현재 실력선 = 몰입 난이도 위치"* 를 추적해야 함 (2장 직결).

## 1.5.8 정직한 리스크 (세미나 검증 대상)
1. ⚠️ **번역 ≠ 직접 사고**: 한→영 번역 구조는 *번역 능력*만 키울 위험. 목표는 "외국어로 *직접* 사고". → **세미나 1순위.**
2. ⚠️ **퍼즐(닫힌 정답) ≠ 언어(열린 소통)**: 정답 1개 맞추기에 과최적화하면 *유연한 소통*이 아니라 *그 한 정답 찾기*를 훈련시킬 위험. 퍼즐 프레임은 **체화·자동화 엔진엔 정확**하나 *열린 의사소통 역량*은 미보장. 설계 포인트: 부분정답/복수정답 처리. → **세미나 질문.** (#1과 같은 뿌리)
3. **알림 피로**: 능동형 말걸기는 잘못하면 짜증. 에이전트가 "말 걸 권리"를 데이터로 벌어야 함.

---

---

# 1.6장. 현재 디자인(Figma) 매핑

실제 제품 디자인(영어의숲 Design file)을 확인 → 우리 설계가 어디에 꽂히는지.

## 1.6.1 확인된 핵심 학습 루프 (= Stage 1, 이미 완성)
```
기록하기(일기+사진)  →  Today'sExpressions(표현)  →  Loading/Making(문장 생성)
  →  PracticeSpeaking(말하기, "○○님의 말을 듣고 있어요")  →  Speaking-Score(채점)
  →  Loading/Feedback  →  AnalysingResult(피드백)  →  Done
```
- 산출은 **음성(speaking)** 기반 → 타이핑보다 *자동화(체화)에 유리*.
- 프롬프트는 **한국어 문장** → 1.5.8 리스크 #1(번역≠직접사고)이 실물로 확인됨.

## 1.6.2 설계 개념 ↔ 디자인 매핑
| 우리 개념 | Figma에 존재 | 비고 |
|---|---|---|
| Stage 1 워크플로우 | 전체 학습 루프 화면 | ✅ 완성 |
| 삶 모델 콜드스타트(2.6 #3) | **관심사 선택** 화면 | ✅ 콜드스타트 답이 디자인에 있음 |
| 학습자 모델 인벤토리 | 단어장 / 나의표현 / 스크랩 | ✅ UI로 노출됨 |
| 성과 신호(갱신) | Speaking-Score / 피드백 | ✅ 신호원 존재 |
| 망각곡선/복습 | 복습 섹션 | ✅ (현재 룰기반 추정) |

## 1.6.3 ⭐ 에이전트 진입점 = StudySettings
현재 **Level / Count / Type을 유저가 수동 설정**(StudySettings 화면).
> = **L1→L2 핸드오프 지점.** 지금은 사람이 난이도 결정(워크플로우) → 에이전트가 *몰입 난이도 자동 보정*.
> SDT 자율성 위해 수동 설정은 **오버라이드**로 유지 (AI 추천 + 사용자 조정).

## 1.6.4 Stage 3(능동형)은 디자인에 아직 없음
모든 화면이 반응형. AI가 먼저 말 거는 화면 없음 → 로드맵(반응형→능동형) 확인, Stage 3은 미래 영역.

---

# 1.7장. PoC 정합성 — Solar 기반 소크라틱 튜터 과제 접목

목표 PoC: **"Solar LLM 기반 소크라틱(Socratic) 학습 AI 튜터 Agent 개발"** (교육 특화 LLM Agent / NFM센터·Edu팀)
세부과제(모두수행): ① 학습자 의도·오개념 진단 모델 ② 소크라틱 발문 생성 엔진 ③ 개인화 학습이력 RAG

> **주객전도 아님**: 세부과제 3개 = 우리가 설계한 **Stage 2 에이전트 두뇌** 그 자체. 외부 정의가 우리 에이전트 해부도와 일치(외부 검증).

## 1.7.1 세부과제 ↔ 우리 컴포넌트
| PoC 과제 | 우리 컴포넌트 | 변화 |
|---|---|---|
| ① 의도·오개념 진단 | 인지/진단 + 학습자 모델 | "약점" → **"오개념(misconception)"** 으로 정밀화 |
| ② 소크라틱 발문 생성 | 행동/도구 = 피드백 단계 | "교정" → **"유도 질문"** 으로 업그레이드 |
| ③ 개인화 학습이력 RAG | 기억(memory) ③ | 구현방식 = **RAG로 확정** |

## 1.7.2 접목 지점 = 피드백 순간에만 (전체 루프 X)
```
[현재]    산출 → 채점 → 피드백("정답은 X")
[소크라틱] 산출(오류) → 진단(왜?) → 유도 질문("어제 일이죠? 그럼 시제는?")
           → 스스로 알아챔 → 자가수정 → 확인
```
근거: **노티싱 가설(Schmidt)** / 자가수정 > 정답받기(desirable difficulty) / **SDT 유능감 증폭**(스스로 깨닫는 쾌감 = 우리 동기엔진 강화) / 올바른 발문엔 오개념 진단(①)이 전제.

## 1.7.3 ⚠️ 긴장 + 안전선 (주객전도 방지)
- 소크라틱 = 느리고 개념적/대화적. 우리 코어 = 빠른 반복 산출·5분 습관·몰입. **최적화 대상이 다름.**
- 위험: "풀(full) 소크라틱 대화 튜터"를 통째로 깔면 산출 루프(정체성)가 느려지고 몰입·습관 붕괴.
- **안전선**: 산출 루프 = **척추(spine)**, 소크라틱 = **선택적(dosed) 피드백 레이어**.
- **새 에이전트 결정**: *"반복되는 오개념이라 대화로 고칠 가치가 있나(→소크라틱) vs 빠르게 교정하고 리듬 유지(→quick-fix)?"* = 몰입↔습관 균형 레버.

## 1.7.4 제약: Solar LLM
- 현재 GPT-4o → PoC는 **Solar(업스테이지) 기반** 요구 → **5장에 모델 추상화 레이어** 필요(provider 교체 가능).
- 긍정: Solar 한국어 강점 ↔ 한국인 학습자·한국어 진단·발문 = 좋은 궁합.

---

# 2장. 학습자 모델 (v0)

> 정통 개념: **학생 모델(Student Model)** — 지능형 교수시스템(ITS)의 핵심. 숙련도 추적 = **지식 추적(Knowledge Tracing / BKT)**. 우리는 이 정통 위에 LLM을 얹는다.

## 2.0 학습자 모델의 임무
에이전트의 핵심 결정(target selection + 몰입 난이도 + 변주 정도)을 내리는 **근거 데이터**.
> 한 줄: *"이 사람의 실력선·약점·삶·습관을 들고 있어서, 다음 퍼즐을 어디에 둘지 정하게 해준다."*

## 2.1 세 얼굴
| 얼굴 | 담는 것 | 무엇을 가능케 하나 |
|---|---|---|
| ① **학습 모델** | 실력선, 문법/어휘 숙련도, 약점, 회피, 망각곡선 | target selection, 몰입 난이도, 변주 정도, 복습 |
| ② **삶 모델** | 관심사, 최근 사건, 반복 인물/장소/루틴 | 개인화 콘텐츠, 능동형 말걸기(관계성) |
| ③ **습관 모델** | 스트릭, 접속 시간대, 최근 활동 | 지속(retention), 능동형 *타이밍* |

## 2.2 ① 학습 모델 — 스키마 (v0)
- **전체 실력선(proficiency)**: 몰입 기준 난이도를 잡는 연속 추정값 (예: 내부 스칼라 / CEFR 태깅)
- **스킬 인벤토리(문법/구문 단위)**: 각 항목별
  - 숙련도(consolidation): **이진 X → 연속/누적값** (변형을 몇 개나 성공했나)
  - 성공/실패 이력, 마지막 노출, 다음 복습 예정(망각곡선)
- **어휘 인벤토리**: 접한/생산한 단어, 숙련도, 마지막 노출
- **약점·회피 패턴**: 반복 오류, 의도적으로 피하는 구조

> 핵심: 숙련도는 **"X를 몇 가지 변형까지 성공했나"** = 변주 메커니즘이 직접 읽고 갱신하는 값.

## 2.3 ② 삶 모델 / ③ 습관 모델 (v0, 개략)
- 삶: 일기에서 추출한 토픽·사건·반복 엔티티(인물/장소). 능동형의 *할 말* 재료.
- 습관: 세션 빈도·시간대 → 능동형 *언제* 말 걸지 + 지속 신호.

## 2.4 갱신 메커니즘 (매 턴)
```
유저 응답 ──> 신호 추출(정답/오답, 반응시간, 힌트사용, 회피) ──> 학습자 모델 갱신
  성공(변형 포함) → 숙련도 ↑, 변주 폭 ↑ 여지
  오답           → 숙련도 ↓ + 복습 예약
  회피 감지      → 약점 플래그
  (= 에이전트의 ⑥성찰 / B루프)
```

## 2.5 변주 메커니즘과의 연결 (1.5 + 본 장)
에이전트가 매 턴 정하는 변주 3결정 — 모두 학습 모델을 읽어서 정함:
| 결정 | 근거(학습 모델) |
|---|---|
| 무엇을 **불변**으로 | 지금 굳히는 중인 문법(중간 숙련도 항목) |
| 무엇을 **변주** | 단어/맥락/구조 — 약점·신규 어휘 끼워넣기 |
| **얼마나** 비틀기 | 실력선 대비 몰입 난이도 (적으면 암기/많으면 몰입깨짐) |

## 2.6 미해결 결정 (사용자 판단 필요 / 세미나 검증)
1. **숙련도 표현 방식**: 이진 vs **연속(BKT식 확률)** vs 누적(성공 변형 수). — *추천: 연속+변형카운트* (몰입 난이도·변주에 둘 다 연속 신호 필요)
2. **스킬 추적 단위(granularity)**: 문법 구문? 어휘? 스킬? CEFR 태깅? — *세미나 1순위 질문이기도 함*
3. **콜드 스타트**: 데이터 없는 첫 사용자의 초기 실력선을 어떻게 추정? (초기 진단 / 일기 난이도 추정)

<!-- 3장 에이전트 루프부터 이어서 작성 -->
