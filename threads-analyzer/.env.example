# Path to the persistent browser profile used by Playwright.
# Created on first headful run; subsequent runs reuse the login session.
THREADS_USER_DATA_DIR=./.threads-session

# Required for AI analysis (Phase 2+). Not used by the scraper alone.
# Get one from https://platform.openai.com/api-keys
OPENAI_API_KEY=
